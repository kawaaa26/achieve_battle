<%= form_with(model: short_term_goal, local: true) do |form| %>
  <% if short_term_goal.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(short_term_goal.errors.count, "error") %> prohibited this short_term_goal from being saved:</h2>

      <ul>
      <% short_term_goal.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :detail %>
    <%= form.text_area :detail %>
  </div>

  <div class="field">
    <%= form.label :experience %>
    <%= form.text_field :experience %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
